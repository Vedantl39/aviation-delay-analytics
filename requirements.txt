pandas>=2.0
numpy>=1.24
streamlit>=1.30
plotly>=5.18
# sqlite3 is Python's standard library — no install needed
#
# Optional, only needed if you point load_to_postgres.py at a real
# Postgres instance instead of the default SQLite demo db:
# sqlalchemy>=2.0
# psycopg2-binary>=2.9
